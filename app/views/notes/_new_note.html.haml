.row
	.post-form
		=form_for(@note = Note.new, :url => course_notes_path(@course), :remote => true, :validate => true, :html => {:multipart => true, :id => 'create_post_form'}) do |f|
			=f.text_area :description, :placeholder => "Note", :rows => "5", :id => "styled"
			=f.hidden_field :user_id, :value => current_user.id
			=f.hidden_field :course_id, :value => @course.id
			=f.submit :class => "small black button", :value => "POST"