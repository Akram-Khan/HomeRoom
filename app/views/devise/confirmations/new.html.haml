.content
	.section
		.row
	.section
		.row
	.section
		.row
	.section
		.row
	.section
		.row
	.section
		.row
	.section
		.row
	.section
		.row
			.two.columns
			.nine.columns
				%h4 Resend confirmation instructions
				= form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post }) do |f|
					=f.text_field :email, :placeholder => "Email", :class => "custom_textarea_no_width", :size => "50"
					=f.submit :class => "small black button", :value => "Resend confirmation instructions"
				= render :partial => "devise/shared/links"
			.one.columns